# Configuration des modèles Ollama pour MCP
# Modèles optimisés pour RAG et analyse Lightning Network

# Modèle principal pour analyse
FROM llama3.1:8b

# Configuration système
SYSTEM """
Tu es un assistant expert en Lightning Network et Bitcoin, spécialisé dans :
- L'analyse des canaux Lightning
- L'optimisation des fees et liquidity management
- La génération de rapports détaillés
- L'analyse de données blockchain

Tu utilises un style professionnel et précis, avec des données factuelles.
Toujours fournir des recommandations actionables.
"""

# Paramètres d'optimisation pour production
PARAMETER temperature 0.1
PARAMETER top_p 0.9
PARAMETER top_k 40
PARAMETER repeat_penalty 1.1
PARAMETER num_ctx 4096
PARAMETER num_predict 1024

# Template de conversation
TEMPLATE """{{ if .System }}<|start_header_id|>system<|end_header_id|>

{{ .System }}<|eot_id|>{{ end }}{{ if .Prompt }}<|start_header_id|>user<|end_header_id|>

{{ .Prompt }}<|eot_id|>{{ end }}<|start_header_id|>assistant<|end_header_id|>

{{ .Response }}<|eot_id|>"""