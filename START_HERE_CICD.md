╔══════════════════════════════════════════════════════════════════╗
║                                                                  ║
║     🚀 CI/CD AUTOMATISÉ - GUIDE DE DÉMARRAGE                     ║
║     Push → Deploy automatique en production                      ║
║                                                                  ║
╚══════════════════════════════════════════════════════════════════╝

📅 Date: 17 octobre 2025
⚡ Temps de setup: 10-15 minutes
✅ Status: 100% Prêt à l'emploi

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📦 CE QUI A ÉTÉ IMPLÉMENTÉ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ Workflows GitHub Actions (3)
   • deploy-production.yml    → Déploiement automatique
   • tests.yml                → Tests sur PR
   • rollback.yml             → Rollback en 1 clic

✅ Scripts de déploiement
   • ci_deploy.sh             → Script serveur
   • check_cicd_setup.sh      → Vérification configuration

✅ Documentation complète
   • CICD_QUICKSTART.md       → Guide 10 minutes
   • docs/CICD_SETUP.md       → Configuration détaillée
   • docs/DEPLOYMENT_RUNBOOK.md → Procédures ops

✅ Templates GitHub
   • Pull Request template
   • Issue deployment template

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎯 DÉMARRAGE RAPIDE - 3 ÉTAPES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌─────────────────────────────────────────────────────────────────┐
│ ÉTAPE 1 : GÉNÉRER LA CLÉ SSH (2 min)                            │
└─────────────────────────────────────────────────────────────────┘

# Sur votre machine locale
ssh-keygen -t ed25519 -C "github-actions@mcp" -f ~/.ssh/github-actions-mcp

# Afficher la clé publique (pour le serveur)
cat ~/.ssh/github-actions-mcp.pub

# Afficher la clé privée (pour GitHub)
cat ~/.ssh/github-actions-mcp

┌─────────────────────────────────────────────────────────────────┐
│ ÉTAPE 2 : CONFIGURER GITHUB SECRETS (3 min)                     │
└─────────────────────────────────────────────────────────────────┘

1. Aller sur GitHub → Repository → Settings
2. Secrets and variables → Actions
3. New repository secret

Ajouter ces 4 secrets :

┌────────────────────────┬─────────────────────────────────────┐
│ Nom du secret          │ Valeur                              │
├────────────────────────┼─────────────────────────────────────┤
│ HOSTINGER_SSH_KEY      │ Contenu de github-actions-mcp       │
│                        │ (clé privée COMPLÈTE)               │
├────────────────────────┼─────────────────────────────────────┤
│ HOSTINGER_HOST         │ 147.79.101.32                       │
├────────────────────────┼─────────────────────────────────────┤
│ HOSTINGER_USER         │ feustey                             │
├────────────────────────┼─────────────────────────────────────┤
│ HOSTINGER_DEPLOY_DIR   │ /opt/mcp                            │
└────────────────────────┴─────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ ÉTAPE 3 : CONFIGURER LE SERVEUR (5 min)                         │
└─────────────────────────────────────────────────────────────────┘

# Se connecter au serveur
ssh feustey@147.79.101.32

# Ajouter la clé publique
echo "COLLER_LA_CLE_PUBLIQUE_ICI" >> ~/.ssh/authorized_keys

# Créer les répertoires
sudo mkdir -p /opt/mcp /opt/mcp-backups
sudo chown $USER:$USER /opt/mcp /opt/mcp-backups

# Vérifier groupe docker
groups | grep docker
# Si absent:
sudo usermod -aG docker $USER
# Puis se déconnecter et reconnecter

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🧪 TESTER LE CI/CD
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌─────────────────────────────────────────────────────────────────┐
│ TEST 1 : VÉRIFIER LA CONFIGURATION                              │
└─────────────────────────────────────────────────────────────────┘

./scripts/check_cicd_setup.sh

✓ Devrait afficher : "CI/CD setup is complete!"

┌─────────────────────────────────────────────────────────────────┐
│ TEST 2 : PREMIER DÉPLOIEMENT                                    │
└─────────────────────────────────────────────────────────────────┘

# Option A : Push sur main (déploiement automatique)
git add .
git commit -m "feat: enable CI/CD"
git push origin main

# Option B : Déploiement manuel
1. GitHub → Actions
2. "Deploy to Hostinger Production"
3. Run workflow

┌─────────────────────────────────────────────────────────────────┐
│ TEST 3 : SUIVRE LE DÉPLOIEMENT                                  │
└─────────────────────────────────────────────────────────────────┘

1. GitHub → Onglet Actions
2. Cliquer sur le workflow run en cours
3. Voir les logs en temps réel

Durée attendue : 8-12 minutes

┌─────────────────────────────────────────────────────────────────┐
│ TEST 4 : VÉRIFIER LE RÉSULTAT                                   │
└─────────────────────────────────────────────────────────────────┘

# Test API
curl https://api.dazno.de/api/v1/health

# Attendu : {"status":"healthy",...}

# Sur le serveur
ssh feustey@147.79.101.32
cd /opt/mcp
sudo docker-compose -f docker-compose.production.yml ps

# Tous les containers doivent être "Up (healthy)"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 COMPRENDRE LE WORKFLOW
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌──────────────────────────────────────────────────────────────┐
│ DÉPLOIEMENT AUTOMATIQUE (8-12 min)                           │
└──────────────────────────────────────────────────────────────┘

1. 🧪 Tests & Validation (3-5 min)
   ├── Tests unitaires avec pytest
   ├── Linting avec flake8
   └── Validation docker-compose

2. 🐳 Build & Push Docker (2-4 min)
   ├── Build image Docker
   ├── Tag automatique (latest, main-SHA)
   └── Push sur GitHub Container Registry

3. 🚀 Deploy to Hostinger (2-3 min)
   ├── Connexion SSH au serveur
   ├── Backup automatique
   ├── Pull nouvelle image
   ├── Redémarrage des services
   └── Health check

4. 🔥 Smoke Tests (1 min)
   ├── Test API health endpoint
   ├── Test documentation
   └── Vérification containers

┌──────────────────────────────────────────────────────────────┐
│ EN CAS D'ÉCHEC : ROLLBACK AUTOMATIQUE                        │
└──────────────────────────────────────────────────────────────┘

Si health check échoue :
→ Restauration automatique du backup
→ Redémarrage avec version précédente
→ Notification de l'échec

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔄 UTILISATION AU QUOTIDIEN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌─────────────────────────────────────────────────────────────────┐
│ WORKFLOW DE DÉVELOPPEMENT RECOMMANDÉ                            │
└─────────────────────────────────────────────────────────────────┘

# 1. Créer une branche feature
git checkout -b feature/ma-fonctionnalite

# 2. Développer et tester localement
# ... modifications ...
pytest tests/

# 3. Commit et push
git add .
git commit -m "feat: description de la fonctionnalité"
git push origin feature/ma-fonctionnalite

# 4. Créer une Pull Request sur GitHub
# → Tests automatiques s'exécutent

# 5. Code review et merge
# → Déploiement automatique en production

# 6. Vérifier le déploiement
curl https://api.dazno.de/api/v1/health

┌─────────────────────────────────────────────────────────────────┐
│ ROLLBACK MANUEL                                                 │
└─────────────────────────────────────────────────────────────────┘

Si vous devez revenir en arrière :

1. GitHub → Actions
2. "Rollback Production" workflow
3. Run workflow
4. Entrer "latest" (ou un timestamp spécifique)
5. Confirmer

Durée : 2-3 minutes

┌─────────────────────────────────────────────────────────────────┐
│ VOIR LES LOGS                                                   │
└─────────────────────────────────────────────────────────────────┘

# Sur GitHub
GitHub → Actions → Sélectionner un run → Voir les logs

# Sur le serveur
ssh feustey@147.79.101.32
cd /opt/mcp
sudo docker-compose -f docker-compose.production.yml logs -f

# Logs d'un service spécifique
sudo docker-compose -f docker-compose.production.yml logs -f mcp-api-prod

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🛠️ COMMANDES UTILES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Vérifier la configuration CI/CD
./scripts/check_cicd_setup.sh

# Voir les backups disponibles
ssh feustey@147.79.101.32
ls -lh /opt/mcp-backups/

# Status des containers
ssh feustey@147.79.101.32
cd /opt/mcp
sudo docker-compose -f docker-compose.production.yml ps

# Redémarrer un service
sudo docker-compose -f docker-compose.production.yml restart mcp-api-prod

# Voir l'utilisation des ressources
sudo docker stats --no-stream

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🐛 DÉPANNAGE RAPIDE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌─────────────────────────────────────────────────────────────────┐
│ ❌ "Permission denied (publickey)"                              │
└─────────────────────────────────────────────────────────────────┘

# Vérifier que la clé publique est sur le serveur
ssh feustey@147.79.101.32
cat ~/.ssh/authorized_keys | grep github-actions

┌─────────────────────────────────────────────────────────────────┐
│ ❌ "docker: permission denied"                                  │
└─────────────────────────────────────────────────────────────────┘

ssh feustey@147.79.101.32
sudo usermod -aG docker $USER
# Déconnexion/reconnexion nécessaire

┌─────────────────────────────────────────────────────────────────┐
│ ❌ "Health check failed"                                        │
└─────────────────────────────────────────────────────────────────┘

# Voir les logs de l'API
ssh feustey@147.79.101.32
cd /opt/mcp
sudo docker-compose -f docker-compose.production.yml logs mcp-api-prod

# Tester manuellement
curl http://localhost:8000/api/v1/health

┌─────────────────────────────────────────────────────────────────┐
│ ❌ "Image not found"                                            │
└─────────────────────────────────────────────────────────────────┘

# Vérifier que l'image a été poussée sur GHCR
# GitHub → Packages → Vérifier que 'mcp' existe

# Se connecter à GHCR sur le serveur
ssh feustey@147.79.101.32
echo "VOTRE_GITHUB_TOKEN" | sudo docker login ghcr.io -u VOTRE_USERNAME --password-stdin

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📚 DOCUMENTATION COMPLÈTE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌────────────────────────────────┬─────────────────────────────────┐
│ Document                       │ Description                     │
├────────────────────────────────┼─────────────────────────────────┤
│ CICD_QUICKSTART.md             │ ⭐ Guide 10 minutes             │
├────────────────────────────────┼─────────────────────────────────┤
│ docs/CICD_SETUP.md             │ 📖 Configuration complète       │
├────────────────────────────────┼─────────────────────────────────┤
│ docs/DEPLOYMENT_RUNBOOK.md     │ 🔧 Procédures opérationnelles   │
├────────────────────────────────┼─────────────────────────────────┤
│ .github/README.md              │ 📋 Documentation workflows      │
├────────────────────────────────┼─────────────────────────────────┤
│ CICD_IMPLEMENTATION_COMPLETE.md│ ✅ Résumé implémentation        │
└────────────────────────────────┴─────────────────────────────────┘

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✨ AVANTAGES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ Déploiement automatique      → Push to deploy en 10 min
✅ Tests automatiques            → Détection précoce des bugs
✅ Rollback facile               → 1 clic pour revenir en arrière
✅ Backups automatiques          → Sécurité avant chaque déploiement
✅ Health checks                 → Validation automatique
✅ Logs traçables                → Historique complet
✅ Notifications (optionnel)     → Slack, Email

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎯 PROCHAINES ÉTAPES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. ✅ Configurer les secrets GitHub
   → Voir section "ÉTAPE 2" ci-dessus

2. ✅ Préparer le serveur
   → Voir section "ÉTAPE 3" ci-dessus

3. ✅ Tester avec un premier déploiement
   → git push origin main

4. ✅ Surveiller le premier déploiement
   → GitHub Actions → Suivre les logs

5. ✅ Vérifier que tout fonctionne
   → curl https://api.dazno.de/api/v1/health

6. 📖 (Optionnel) Lire la doc complète
   → docs/CICD_SETUP.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎉 FÉLICITATIONS !

Vous disposez maintenant d'un pipeline CI/CD moderne et robuste !

👉 Commencez par : CICD_QUICKSTART.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📅 Implémenté le: 17 octobre 2025
✅ Status: Production Ready
🚀 Prêt à déployer !

