[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Afficher les messages de dépréciation
filterwarnings =
    ignore::DeprecationWarning:distutils.*
    ignore::DeprecationWarning:pkg_resources.*
    default::DeprecationWarning
    default::PendingDeprecationWarning

# Configuration pour les tests asynchrones
asyncio_mode = auto

# Chemin pour les fixtures temporaires
cache_dir = .cache/pytest

# Options de rapports (--doctest-modules désactivé par défaut pour éviter échecs CI sur modules tiers)
addopts =
    --verbose
    --tb=native
    --junitxml=reports/junit.xml